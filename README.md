# Homebrew Tap for DimiCheck

Install DimiCheck Mac:

```bash
brew tap DimiCheck/dimicheck
brew install --cask dimicheck-mac
```

One-line install:

```bash
brew install --cask DimiCheck/dimicheck/dimicheck-mac
```
